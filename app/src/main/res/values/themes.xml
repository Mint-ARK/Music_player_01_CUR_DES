<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Music_01" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="searchViewStyle">@style/MySearchViewStyle</item> <!-- 添加这一行 -->
    </style>

    <style name="Theme.Music_01" parent="Base.Theme.Music_01" />

    <!-- 添加这个新的样式 -->
    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- 设置提示词的颜色 -->
        <item name="android:textColorHint">#808080</item> <!-- 灰色 -->
    </style>
    <style name="popwin_anim_style" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/pop_exit_anim</item>
    </style>
</resources>
